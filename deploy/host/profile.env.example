# Copy to deploy/host/profile.env. That path is ignored by git.
# This file must never contain a password, MAC address, or device identifier.
TRAIL_ADMIN_USER=trailadmin
TRAIL_LAN_CIDR=REPLACE_WITH_TRUSTED_IPV4_LAN_CIDR
